# Claude Code runtime (Phase 0 only — deleted at Phase 1)
.claude/settings.local.json

# MCP config (contains API tokens — use .mcp.json.example as template)
.mcp.json

# Per-user config (use .example templates as basis — `cp config/X.md.example config/X.md`)
config/user.md
config/memory.md
config/connectors.md
config/*.log

# OS files
.DS_Store

# Credentials (never commit)
*.env
token.json
